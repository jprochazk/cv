{
    "scripts": {
        "build": "node ./build.js",
        "prestart": "npm run build",
        "start": "http-server ./dist"
    },
    "license": "MIT",
    "devDependencies": {
        "http-server": "^0.12.3",
        "terser": "^5.7.0"
    }
}
